[build-system]
# Minimum requirements for the build system to execute.
requires = ["setuptools"]
[tool.setuptools.packages.find]
where = ["."]
[project]
name = "cascade-unet-vessels"
version = "0.1.0"